# Corpus français

La branche `fr` contient uniquement le corpus français canonique décrit par
`fr-FR/manifest.json`. Le catalogue anglais n'est pas dupliqué ici : il se
trouve sur la branche `en`, sous `locales/en-US/`.
